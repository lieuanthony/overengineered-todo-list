{
  "name": "overengineered-todo-list",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "dev": "docker compose up -d && concurrently --kill-others-on-fail \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "format": "npm run format --workspace=backend && npm run format --workspace=frontend",
    "test": "npm run test --workspace=backend",
    "stop": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "prettier": "^3.8.1"
  }
}